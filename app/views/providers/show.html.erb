<p style="color: green"><%= notice %></p>

<%= render @provider %>

<div>
  <%= link_to t(:edit), edit_provider_path(@provider) %> |
  <%= link_to t(:back), providers_path %>

  <%= button_to t(:delete), @provider, method: :delete %>
</div>
